# concept-drift-monitoring-system
This project builds an ML monitoring system that detects when a deployed machine-learning model becomes unreliable due to changes in incoming data distributions (concept drift), using statistical drift metrics and model confidence analysis.
